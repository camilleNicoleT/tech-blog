<article class="post">
  <div class="title">
   <h3><a href="{{post_url}}" target="blank">{{title}}</a> </h3> 
     <span>{{text}}</span>
    <span>{{post_url}}</span>
  </div>
  <div class="meta">
    Created by {{user.username}} on {{format_date created_at}}
    |
    <a href="/post/{{id}}">{{comments.length}} {{format_plural "comment" comments.length}}</a>
  </div>
</article>